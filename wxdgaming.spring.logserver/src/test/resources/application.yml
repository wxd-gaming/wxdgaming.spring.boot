core:
  executor:
    basic:
      coreSize: 2
      maxQueueSize: 5000
      queuePolicy: AbortPolicy


server:
  port: 8888
  undertow:
    io-threads: 4
    worker-threads: 200
    buffer-size: 1024
    direct-buffers: true

spring:
  profiles:
    active: pgsql10
